
webnovel-crosspoint-xteinkx4

A small Unraid Docker workflow that converts saved webnovel .html files into e-reader-friendly .epub files for the Xteink X4 / CrossPoint custom firmware setup.

I built this because normal webpage-to-EPUB tools gave me EPUBs that looked fine online but did not behave properly on my e-reader. The book would open, but the second “page” could become one massive scrollable document instead of clean page turns. This project fixes that by splitting the EPUB into smaller internal sections.

What this does

Saved .html file↓Unraid input folder↓Docker watcher sees the file↓HTML is cleaned and converted↓EPUB is split into smaller internal parts↓EPUB goes to Calibre ingest folder↓Calibre automatically imports it↓Book appears through OPDS↓Xteink X4 / CrossPoint reader downloads it

What this does not do

This project does not scrape websites by URL.

It only works with saved .html or .htm files that you put into the input folder.

I use the SingleFile extension on Firefox to save the webpage as a single HTML file.

I also adjusted the naming. By default, SingleFile saves with date and time, but I removed that so that the file name now just shows the chapter title

Current working environment

This is the setup I currently have working:

Unraid server
Docker
Calibre
Calibre automatic adding folder
Calibre OPDS / content server
Xteink X4 with CrossPoint custom firmware

The project folder on my Unraid server is:

/mnt/user/data/webnovel-epub

Authorship / ChatGPT acknowledgement

Most of the code, Docker commands, folder structure, and setup instructions in this project were generated by ChatGPT during a live troubleshooting and setup session.

I copied and followed most of the commands on my Unraid server. I tested the workflow, found problems during real use, and fixed or adjusted things where needed.

I understand the overall workflow and most of what the code is doing, but the actual Python converter code and much of the Docker/setup script were written by ChatGPT, not manually written from scratch by me.

This repository is shared as the working result of that process.

GitHub structure

```bash
pip install esptool
```
webnovel-crosspoint-xteinkx4/
|-- README.md
|-- .gitignore
|--- app/
│   ├── Dockerfile
│   ├── Dockerfile.watcher
│   ├── html_to_epub.py
│   └── watcher.sh
└── examples/
    └── test-chapter.html
    '''

Create the folders on Unraid

mkdir -p /mnt/user/data/webnovel-epub/input
mkdir -p /mnt/user/data/webnovel-epub/output
mkdir -p /mnt/user/data/webnovel-epub/app
mkdir -p /mnt/user/data/webnovel-epub/processing
mkdir -p /mnt/user/data/webnovel-epub/imported-html
mkdir -p /mnt/user/data/webnovel-epub/failed
mkdir -p /mnt/user/data/webnovel-epub/calibre-ingest
mkdir -p /mnt/user/data/webnovel-epub/logs

Optional permission cleanup:

chown -R nobody:users /mnt/user/data/webnovel-epub
chmod -R 777 /mnt/user/data/webnovel-epub

Folder purpose

input          Put saved .html/.htm files here
processing     Temporary folder while conversion is happening
imported-html  Successfully converted original HTML files
failed         HTML files that failed conversion
calibre-ingest EPUBs ready for Calibre to auto-import
logs           Watcher logs
app            Dockerfiles and scripts

Install the project files on Unraid

Copy the contents of the app/ folder from this repository into:

/mnt/user/data/webnovel-epub/app

So it should look like:

/mnt/user/data/webnovel-epub/app/Dockerfile
/mnt/user/data/webnovel-epub/app/Dockerfile.watcher
/mnt/user/data/webnovel-epub/app/html_to_epub.py
/mnt/user/data/webnovel-epub/app/watcher.sh

Make the watcher executable:

chmod +x /mnt/user/data/webnovel-epub/app/watcher.sh

Build the watcher Docker image

cd /mnt/user/data/webnovel-epub/app
docker build -f Dockerfile.watcher -t webnovel-epub-watcher .

Run the watcher container

docker run -d \
  --name webnovel-epub-watcher \
  --restart unless-stopped \
  --user 99:100 \
  -v /mnt/user/data/webnovel-epub/input:/input \
  -v /mnt/user/data/webnovel-epub/processing:/processing \
  -v /mnt/user/data/webnovel-epub/imported-html:/imported-html \
  -v /mnt/user/data/webnovel-epub/failed:/failed \
  -v /mnt/user/data/webnovel-epub/calibre-ingest:/calibre-ingest \
  -v /mnt/user/data/webnovel-epub/logs:/logs \
  webnovel-epub-watcher

Check that the watcher is running

docker ps | grep webnovel-epub-watcher

Check logs

cat /mnt/user/data/webnovel-epub/logs/watcher.log

Live log:

tail -f /mnt/user/data/webnovel-epub/logs/watcher.log

Connect it to Calibre

In the Calibre Docker container, add this path mapping:

Host path:
/mnt/user/data/webnovel-epub/calibre-ingest

Container path:
/ingest

Then inside Calibre:

Preferences → Adding books → Automatic adding

Set the automatic adding folder to:

/ingest

After that, any EPUB created in calibre-ingest should automatically appear in Calibre.

Normal usage

Save a webpage as .html or .htm.

Put that file into:

/mnt/user/data/webnovel-epub/input

The watcher converts it.

The original HTML moves to:

/mnt/user/data/webnovel-epub/imported-html

The EPUB appears in:

/mnt/user/data/webnovel-epub/calibre-ingest

Calibre imports it.

The book appears through OPDS.

The e-reader can download it.

Important filename note

The EPUB title comes from the uploaded HTML filename.

Example:

7.55 E.html

becomes:

7.55 E.epub

If a file does not have .html or .htm at the end, the watcher will ignore it.

Fix a file with no extension

If a saved file lands in the input folder without .html, run:

find /mnt/user/data/webnovel-epub/input -maxdepth 1 -type f ! -iname "*.html" ! -iname "*.htm" -print0 | while IFS= read -r -d '' f; do
  mv "$f" "$f.html"
done

Stop and remove the watcher

docker stop webnovel-epub-watcher
docker rm webnovel-epub-watcher

Rebuild after editing code

cd /mnt/user/data/webnovel-epub/app
docker build -f Dockerfile.watcher -t webnovel-epub-watcher .

Then restart the container with the same docker run command above.

Notes

The converter removes images by default. This is intentional because the target e-reader setup behaved better with cleaner, lighter EPUB files.

The converter splits the book into smaller internal EPUB sections using this setting inside watcher.sh:

--chunk-size 3500

A smaller chunk size creates more internal EPUB sections. A larger chunk size creates fewer sections.

